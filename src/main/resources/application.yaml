spring:
  application:
    name: donuts
  jackson:
    use-jackson2-defaults: true
    serialization:
      indent-output: true

# BootUI-Dashboard nur unter dem Profil 'dev' aktivieren (Prod/Tests bleiben unberuehrt).
# Start mit Profil dev: mvn spring-boot:run -Dspring-boot.run.profiles=dev  ->  Dashboard unter /bootui
bootui:
  enabled-profiles: dev

#  jackson:
#    mapper:
#      sort-properties-alphabetically: false
